{
    "description": "Test-only dependencies for the Magmodules Channable module. Kept out of the module's own composer.json so the shipped package stays dependency-free, and installed separately because Magento's root autoloader excludes **/Test/** from its classmap, which breaks PHPUnit's own class map.",
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "config": {
        "sort-packages": true
    }
}
